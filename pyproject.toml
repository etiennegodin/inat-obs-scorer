[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "inat_pipeline"
version = "0.0.1"
description = "Data pipeline for inaturalist observations"
authors = [{ name="Etienne Godin", email="etiennegodin@duck.com" }]

dependencies = [
    "argcomplete",
    "pandas",
    "scikit-learn",
    "optuna",
    "mlflow"

]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "pytest-dependency",
    "black",
    "ruff",
]

[project.scripts]   # <-- entry point CLI
inat_pipe = "inat_pipeline.cli:main"


[tool.black]
line-length = 88
target-version = ["py310"]

[tool.ruff]
line-length = 88
target-version = "py310"
select = ["E", "F", "I"]
fix = true

[tool.mypy]
python_version = "3.11"
strict = true

[tool.pytest.ini_options]
addopts = "--cov=src/nukekit --cov-report=term-missing --cov-report=html"
